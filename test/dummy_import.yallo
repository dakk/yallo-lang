interface Dummy {}

function testolordo(): unit {

}

function testolordo2(a: int list, b: nat): nat {
	this.ciao = 12;
	var a: int = 12 + 13 + tesolordo(12);
	//if (13 + 12) { return 12; } else {return b;}
	var c: int = if (12 == 13) then 1 else 2;
	return b;
}

const a : string = "ciao";
type states = enum (CIAO | MONDO);

type test_tuple = (int, nat);

type test_tuple_map = (int, nat) map;


type test_tuple_map2 = (int, test_tuple) map;

// qua c'e' il problema che non possiamo usare tipi composti se non in alias
type test_tuple_map_var = (int, nat option) map;

type c = nat option;
type test_tuple_map3 = (int, c) map;

type a = record {
	a : int option;
	b: nat;
};