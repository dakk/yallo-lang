import "test/fa12.yallo";

const tzTokenAddr: address = "12321312";

contract usingTezToken {
	field balance: int;

	entry test () {
		var x: FA12 = contract<FA12>(tzTokenAddr);
		<= x.getBalance(Tezos.sender, this.balanceHandler);
	}

	entry balanceHandler(b: int) {
		this.balance = b;
	}
}