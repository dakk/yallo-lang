function testFunction (a: int): int {
	let b: int = 12;
	let c: int = 12;
	assert(a>12);
	a * 512 + c
}

contract Test {
	field b: (int, int) map;

	entry testConst () {
		let b: int = 12;
		let c: int = b * 11;
		[]
	}
	entry testVar () {
		let b: int = 12;
		let c: int = 13;
		let (c: int, b: int list) = (12, [12]);
		[]
	}
	entry testAssign () {
		let bb: record { a: int } = { a=12 };
		[]
	}

	entry testCall () {
		this.b.update(12, 15);
		fail("error");
		assert(12 > 14);
		[]
	}
	
	entry testReturn () {
		[]
	}
}